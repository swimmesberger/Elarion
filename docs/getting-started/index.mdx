---
title: Getting started
description: Install Elarion, get one handler onto the wire, and learn the project layout the generators expect.
---

This section takes you from an empty solution to a working, source-generated endpoint. Follow the
three pages in order — each builds on the last.

<Cards>
  <Card title="Installation" href="/docs/getting-started/installation">
    Add the runtime packages and source generators to an application project and an ASP.NET Core host.
  </Card>
  <Card title="Quickstart" href="/docs/getting-started/quickstart">
    Build your first module and handler and call it over a JSON-RPC endpoint — a few minutes, no database.
  </Card>
  <Card title="Project structure" href="/docs/getting-started/project-structure">
    The solution layout and naming conventions the generators rely on, and the dependency direction
    that keeps your application independent of the host.
  </Card>
</Cards>

## Where to go next

<Cards>
  <Card title="Build a real app" href="/docs/tutorial">
    A full end-to-end tutorial: a modular billing backend with a typed React frontend.
  </Card>
  <Card title="Understand the model" href="/docs/concepts">
    Handlers, results, modules, and the source generation that wires them — the mental model behind
    everything else.
  </Card>
</Cards>

<Callout type="warn">
  **Don't learn from pre-generator examples.** Older Elarion codebases predate the current generator
  model: they vendor an older copy of Elarion and hand-write the per-module
  `services.AddXHandlers()` / `AddXServices()` registrations that `[GenerateModuleBootstrapper]` and the
  generated `ConfigureDefaultServices` now emit for you — copying that pattern onto current packages
  duplicates registrations the generator already emits (see [Modules](/docs/concepts/modules)). Use the
  [tutorial](/docs/tutorial) and its compiled billing sample under
  [`samples/Billing`](https://github.com/swimmesberger/Elarion/tree/main/samples/Billing) as the current
  reference instead. One gap to know about: the billing sample authenticates with JWT bearer tokens, so
  the ASP.NET [Identity](/docs/capabilities/identity) path has no sample of its own yet.
</Callout>
