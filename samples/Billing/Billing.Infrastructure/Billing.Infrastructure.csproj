<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Billing sample — intent-only mechanism adapters (the SMTP email sender behind the module's port). The database is application logic and lives in Billing.Application, not here.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Billing.Application\Billing.Application.csproj"/>
  </ItemGroup>

</Project>
